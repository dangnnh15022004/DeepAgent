# All tools migrated to deeptrace_mcp_server.py (MCP server)
# Keeping this file as a stub for future local @tool decorators.

# TODO: ask_knowledge_base — wire up when KB API is ready
# @tool
# async def ask_knowledge_base(question: str, session_id: str) -> str:
#     res = await api_post("", "", {"question": question, "session_id": session_id})
#     ...
